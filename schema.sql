CREATE TABLE USA_Table(
unique_id VARCHAR(100),
race VARCHAR(100),
age_bucket VARCHAR(100),
gender VARCHAR(100),
support_score DECIMAL,
turnout_score DECIMAL
);

CREATE TABLE Model_Universe(
unique_id VARCHAR(100),
race VARCHAR(100),
age_bucket VARCHAR(100),
gender VARCHAR(100),
support_score DECIMAL,
turnout_score DECIMAL
);

CREATE TABLE Age_Universe(
unique_id VARCHAR(100),
race VARCHAR(100),
age_bucket VARCHAR(100),
gender VARCHAR(100),
support_score DECIMAL,
turnout_score DECIMAL
);